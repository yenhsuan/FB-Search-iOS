# Uncomment the next line to define a global platform for your project
# platform :ios, '10.3'

use_frameworks!
target 'FBSearch' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  # Pods for FBSearch
  pod 'SwiftyJSON', '~> 3.1'
  pod 'AlamofireImage', '~> 3.2'
  pod 'SwiftSpinner', '~> 1.2'
  pod 'EasyToast', '~> 2.0'
  pod 'FacebookCore'
  pod 'FacebookLogin'
  pod 'FacebookShare'
end
